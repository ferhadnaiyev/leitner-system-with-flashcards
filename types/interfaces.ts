export interface CardsType {
    id: string;
    termin: string;
    definition: string;
    boxID: number;
}
export interface BoxType {
    id: number;
    name: string;
    boxID: number;
    practiceRequired: number;
}